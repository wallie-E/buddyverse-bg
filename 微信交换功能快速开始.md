# 微信交换功能 - 快速开始指南

## ✅ 已完成的工作

### 1. 数据库层
- ✅ 创建 `wechat_exchange` 表
- ✅ 更新 `notifications` 表支持新的通知类型
- ✅ 执行数据库迁移成功

### 2. 后端 API
- ✅ `wechatExchangeController.js` - 完整的业务逻辑
- ✅ `routes/wechatExchange.js` - API 路由定义
- ✅ 在 `app.js` 中注册路由
- ✅ 支持 4 个核心接口：
  - 获取交换信息
  - 发起/更新交换请求
  - 确认交换（接受/拒绝）
  - 获取我的交换记录列表

### 3. 前端示例代码
- ✅ `WechatExchangeModal.jsx` - React 交换弹窗组件
- ✅ `NotificationList.jsx` - 通知列表组件（集成交换功能）
- ✅ 包含完整的 CSS 样式

### 4. 文档和测试
- ✅ 完整的 API 文档
- ✅ Postman 测试集合
- ✅ 自动化测试脚本
- ✅ 业务流程图

## 🚀 快速开始

### 步骤 1: 确认数据库迁移

数据库迁移已经执行完成！如果需要重新执行：

```bash
node scripts/addWechatExchange.js
```

### 步骤 2: 启动服务

```bash
npm start
# 或
npm run dev
```

服务将运行在 `http://localhost:8080`

### 步骤 3: 测试 API

#### 方法 1: 使用 Postman

1. 导入 `微信交换API测试集合.postman_collection.json`
2. 设置环境变量：
   - `baseUrl`: http://localhost:8080
   - `token`: 你的 JWT token
   - `targetUserId`: 目标用户 ID
3. 按顺序执行测试

#### 方法 2: 使用 curl

```bash
# 1. 获取交换信息
curl -X GET "http://localhost:8080/api/wechat-exchange/info?targetUserId=2" \
  -H "Authorization: Bearer YOUR_TOKEN"

# 2. 发起交换请求
curl -X POST "http://localhost:8080/api/wechat-exchange/request" \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -d '{"targetUserId": 2, "wechatId": "my_wechat_123"}'

# 3. 确认交换
curl -X POST "http://localhost:8080/api/wechat-exchange/confirm" \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -d '{"exchangeNo": "WX...", "wechatId": "other_wechat_456", "action": "accept"}'
```

#### 方法 3: 使用测试脚本

```bash
# 编辑脚本，设置 token
vi scripts/testWechatExchange.js

# 运行测试
node scripts/testWechatExchange.js
```

## 📁 项目文件结构

```
project/
├── controllers/
│   └── wechatExchangeController.js    # 微信交换控制器
├── routes/
│   └── wechatExchange.js              # 微信交换路由
├── database/
│   └── migrations/
│       └── add_wechat_exchange.sql    # 数据库迁移 SQL
├── scripts/
│   ├── addWechatExchange.js           # 迁移执行脚本
│   └── testWechatExchange.js          # 自动化测试脚本
├── examples/
│   ├── WechatExchangeModal.jsx        # React 弹窗组件
│   └── NotificationList.jsx           # 通知列表组件
├── 微信交换功能文档.md                 # 完整文档
├── 微信交换功能快速开始.md             # 本文件
└── 微信交换API测试集合.postman_collection.json  # Postman 测试集合
```

## 🎯 核心功能

### 1. 双向确认机制
- 发起人发送请求时提供微信号
- 接收人确认时提供微信号
- 双方都确认后才能看到对方微信号

### 2. 实时通知
- 发起请求时通知接收人
- 确认交换时通知发起人
- 支持通知列表查看和已读标记

### 3. 状态管理
- **待确认 (0)**: 已发起，等待对方确认
- **已完成 (1)**: 双方都确认，可查看微信号
- **已拒绝 (2)**: 接收人拒绝交换
- **已过期 (3)**: 超时未确认（需自行实现定时任务）

### 4. 重新发起
- 发起人可以更新微信号并重新发送请求
- 更新确认时间，接收人收到新通知

## 📊 API 端点

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/wechat-exchange/info` | 获取交换信息 |
| POST | `/api/wechat-exchange/request` | 发起/更新交换请求 |
| POST | `/api/wechat-exchange/confirm` | 确认交换（接受/拒绝）|
| GET | `/api/wechat-exchange/my-exchanges` | 获取我的交换记录 |

## 🎨 前端集成

### 在用户资料页添加交换按钮

```jsx
import WechatExchangeModal from './components/WechatExchangeModal';

function UserProfile({ userId, userName }) {
  const [showModal, setShowModal] = useState(false);

  return (
    <div>
      <button onClick={() => setShowModal(true)}>
        交换微信
      </button>

      <WechatExchangeModal
        visible={showModal}
        targetUserId={userId}
        targetUserName={userName}
        onClose={() => setShowModal(false)}
        onSuccess={() => alert('操作成功')}
      />
    </div>
  );
}
```

### 在通知中心处理交换请求

```jsx
import NotificationList from './components/NotificationList';

function NotificationPage() {
  return <NotificationList />;
}
```

## 🔍 业务流程

### 场景 1: 首次交换

1. **用户 A** 点击"交换微信"按钮
2. 系统调用 `GET /info`，发现无记录
3. 弹窗让用户输入微信号
4. 调用 `POST /request` 发起请求
5. 系统创建通知给**用户 B**
6. **用户 B** 在通知中心看到请求
7. 点击通知，输入微信号并确认
8. 调用 `POST /confirm` 接受请求
9. 系统通知**用户 A** 交换成功
10. 双方都能看到对方微信号

### 场景 2: 重新发起

1. **用户 A** 再次点击"交换微信"
2. 系统调用 `GET /info`，发现有待确认的记录
3. 弹窗提示"等待对方确认，是否重新发送？"
4. 用户点击确定
5. 调用 `POST /request` 并设置 `isUpdate: true`
6. 系统更新确认时间并发送新通知给**用户 B**

### 场景 3: 查看已完成的交换

1. 用户点击"交换微信"按钮
2. 系统调用 `GET /info`，发现已完成
3. 弹窗直接展示对方微信号
4. 用户可以复制去添加好友

## 🔐 安全性

- ✅ JWT 认证保护所有接口
- ✅ 权限验证：只能操作自己的交换记录
- ✅ 输入验证：防止恶意输入
- ✅ 隐私保护：微信号只在双方确认后可见
- ✅ 接口限流：防止滥用（已配置 rate-limit）

## 🐛 常见问题

### Q1: 如何获取 JWT token？

```bash
# 登录接口
curl -X POST "http://localhost:8080/api/auth/login" \
  -H "Content-Type: application/json" \
  -d '{"email": "user@example.com", "password": "password123"}'

# 响应中的 data.token 就是需要的 token
```

### Q2: 如何创建测试用户？

```bash
# 注册接口
curl -X POST "http://localhost:8080/api/auth/register" \
  -H "Content-Type: application/json" \
  -d '{
    "email": "test@example.com",
    "password": "password123",
    "nickname": "测试用户"
  }'
```

### Q3: 数据库连接失败？

检查 `config/database.js` 中的数据库配置是否正确。

### Q4: 通知没有发送？

1. 检查 `notifications` 表是否存在
2. 确认通知类型已更新（运行迁移脚本）
3. 查看服务器日志排查错误

## 📝 下一步

### 可选功能扩展

1. **交换过期机制**
   - 添加定时任务，自动将超时未确认的记录标记为已过期
   - 示例：使用 `node-cron` 每天检查一次

2. **撤回请求**
   - 添加接口允许发起人撤回未确认的请求
   - 更新状态并通知接收人

3. **黑名单**
   - 添加用户黑名单表
   - 屏蔽特定用户的交换请求

4. **实时推送**
   - 集成 WebSocket 或 SSE
   - 实时推送通知到前端

5. **数据统计**
   - 统计交换成功率
   - 分析用户活跃度

## 🎉 完成！

恭喜！微信交换功能已经完全实现并可以使用了。

如有任何问题，请查看：
- 📖 [完整文档](./微信交换功能文档.md)
- 🧪 [Postman 测试集合](./微信交换API测试集合.postman_collection.json)
- 💻 [示例代码](./examples/)

祝开发顺利！🚀

